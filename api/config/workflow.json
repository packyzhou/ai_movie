{
  "comment": "`template` names the built-in ComfyUI workflow (API format) inside ./api used to seed a brand-new user. Export it from ComfyUI with `Save (API Format)`. If the file is absent the app still runs - the template list just starts empty and you add one from the Templates page.",
  "template": "video_minimax_h3_t2v.json",
  "limits": {
    "width": { "min": 256, "max": 1920, "step": 32, "default": 1280 },
    "height": { "min": 256, "max": 1920, "step": 32, "default": 704 },
    "duration": { "min": 1, "max": 10, "step": 0.5, "default": 5 }
  }
}
